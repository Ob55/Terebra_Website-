# Endpoint that receives form submissions (Contact, Register, Scoping call).
# Use a static-friendly form backend such as Formspree or Getform, e.g.:
#   https://formspree.io/f/xxxxxxx
# Leave unset in development — forms then show a friendly "not configured" notice
# instead of attempting a network request.
VITE_FORM_ENDPOINT=
